import { PageProps } from "@/types";

export default function DetallesImpresoraPage({ params }: PageProps) {
  const { id } = params;
  
  return (
    <div>
      <h1>Hello Page</h1>
    </div>
  );
}